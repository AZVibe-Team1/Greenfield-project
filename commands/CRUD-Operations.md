Using the seeker.py file in backend/schemas, create CRUD operations for the mongo database
collection 'seeker' in the database 'job-portal'.  CRUD must include create collection, read collection, update collection,
delete collection.  This code will go in the backend/db folder in a file called 'seeker_db_ops.py'.
Then, using the employer.py file in backend/schemas, create CRUD operations for the mongo database collectoni 'employer'
in the database 'job-portal'.  CRUD must include create collection, read collection, update collection,
delete collection. This code will go in the backend/db folder in a file called 'employer_db_ops.py'.